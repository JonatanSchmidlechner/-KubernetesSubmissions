apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todoapp-pvc # name of the volume claim, this will be used in the deployment
spec:
  storageClassName: todoapp-pvc # this is the name of the persistent volume we are claiming
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 800Mi
